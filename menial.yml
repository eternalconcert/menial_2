hosts:
  test.localhost:
    bind: 'localhost'
    port: '8090'
    root: default
    resources: default/pages
  test2.localhost:
    bind: '0.0.0.0'
    port: '8080'
    root: /tmp/
    resources: default/pages
loglevel: info
