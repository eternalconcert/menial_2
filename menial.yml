loglevel: info
hosts:
  localhost:8080:
    root: default/welcomepage
    resources: default/pages
  test.localhost:8080:
    root: /tmp/
    resources: default/pages
ssl:
  4433:
    key: key.pem
    cert: cert.pem
